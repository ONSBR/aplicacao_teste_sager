execucao-calculo-fechamento:
  model: tb_execucao_calculo_fechamento
  fields:
    id:
      column: id
    dataInicio:
      column: dataInicio
    dataFim:
      column: dataFim
    idFechamentoMensal:
      column: idFechamentoMensal
    idCenario:
      column: idCenario
    idTarefa:
      column: idTarefa
    protocolo:
      column: protocolo
  filters:
    byDataInicioFim:
      $and:
        - dataInicio:
            $eq: :dataInicio
        - dataFim:
            $eq: :dataFim

fechamento-mensal:
  model: tb_fechamento_mensal
  fields:
    id:
      column: id
    mes:
      column: mes
    ano:
      column: ano
  filters:
    byData:
      $and:
        - mes:
            $eq: :mes
        - ano:
            $eq: :ano

tarefa-retificacao:
  model: tb_tarefa_retificacao
  fields:
    id:
      column: id
    nome:
      column: nome
  filters:
    byName:
      nome: :nome                      