const Util = require('../../controllers/parse/util');

describe('O util deve', function () {

    it('verificar evento valido para o tipo de parametro HDF', () => {
        expect(validado).toBe(true);
    });

});
