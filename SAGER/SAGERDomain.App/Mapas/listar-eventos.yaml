evento:
  model: tb_evento
  fields:
    id:
      column: id
      required: true
    dataVerificacao:
      column: dt_verificacao
      required: true
    potenciaDisponivel:
      column: vl_potencia_disponivel
      required: true      
  filters:
    byName:
      nome: :nome